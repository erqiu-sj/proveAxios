import { customConfiguration } from '@zealforchange/proveaxios'
export interface wechatRequestConfigurationConfig {
  isFormDate: boolean
  legalMayToWorkWithFormdate: customConfiguration<object>['method'][]
}
